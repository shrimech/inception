services:
  mariadb:
    build: requirements/mariadb
    image: mariadb:v1
    container_name: mariadb
    restart: always
    env_file: .env
    networks:
      - inception
    volumes:
      - mariadb_data:/var/lib/mysql

  wordpress:
    build: requirements/wordpress
    image: wordpress:v1
    container_name: wordpress
    restart: always
    env_file: .env  
    depends_on:
      - mariadb
    networks:
      - inception
    volumes:
      - wordpress_data:/var/www/html

  nginx:
    build: requirements/nginx
    image: nginx:v1
    container_name: nginx
    restart: always
    depends_on:
      - wordpress
    networks:
      - inception
    volumes:
      - wordpress_data:/var/www/html
    ports:
      - "443:443"


volumes:
  mariadb_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/shrimech/data

  wordpress_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/shrimech/data


networks:
  inception:
    driver: bridge
